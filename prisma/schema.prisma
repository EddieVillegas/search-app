generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Student {
  Id Int @default(autoincrement()) @id
  firstName String
  lastName String
  email String @unique
}